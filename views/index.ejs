<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Members Only</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-slate-100 min-h-screen flex flex-col">
    <%- include("./partials/navbar") %>
    <%- include("./partials/alerts") %>

    <main class="flex-1 container mx-auto px-4 py-8">
      <section class="max-w-3xl mx-auto">
        <div class="flex items-center justify-between mb-6">
          <h1 class="text-3xl font-bold">Messages</h1>

          <% if (isLoggedIn) { %>
            <a href="/messages/new" class="px-4 py-2 rounded-full bg-black text-white text-sm font-semibold">
              Create new post
            </a>
          <% } %>
        </div>

        <% if (!messages || messages.length === 0) { %>
          <p class="text-slate-600">No messages yet.</p>
        <% } else { %>
          <div class="space-y-4">
            <% messages.forEach(m => { %>
              <article class="bg-white rounded-lg shadow-sm border p-4">
                <h2 class="text-xl font-semibold mb-2"><%= m.title %></h2>

                <p class="text-slate-800 whitespace-pre-line"><%= m.body %></p>

                <% if (isMember) { %>
                  <div class="mt-3 text-sm text-slate-600">
                    By <span class="font-medium"><%= m.first_name %> <%= m.last_name %></span>
                    • <%= new Date(m.created_at).toLocaleString() %>
                  </div>
                <% } else { %>
                  <div class="mt-3 text-sm text-slate-500 italic">
                    (Anonymous)
                  </div>
                <% } %>
              </article>
            <% }) %>
          </div>
        <% } %>
      </section>
    </main>
  </body>
</html>